---
status: idea
---

# Scenario 004: Custom config directories

Validates that prompts process correctly from non-default folders when `promptDir` is configured in `.dark-factory.yaml`.

## What it proves

Config system works — dark-factory respects custom directory settings.
