---
status: idea
---

# Scenario 005: Three prompts execute in dependency order

Validates that prompts with `depends_on` frontmatter execute in correct order and all complete.

## What it proves

Queue ordering works — dependency resolution produces correct execution sequence.
