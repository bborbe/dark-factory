---
status: idea
---

# Verification gate pauses before completion

Validates that a prompt with `verificationGate: true` pauses at `pending_verification`, waits for human verification, then completes.
